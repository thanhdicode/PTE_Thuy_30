store-dir=.pnpm-store          # Local store for faster access
prefer-offline=true             # Reduce network calls
child-concurrency=3             # Limit concurrent operations
package-import-method=hardlink  # Faster file operations
network-timeout=60000          # Handle slower connections