# yaml-language-server: $schema=https://raw.githubusercontent.com/agents-inc/cli/main/src/schemas/metadata.schema.json
category: api-messaging
slug: webhooks
domain: api
author: "@vince"
displayName: Webhooks
cliDescription: Webhook patterns — receiving, sending, signature verification, and retry logic
usageGuidance: >-
  Use when implementing webhook endpoints or sending webhooks — signature
  verification (HMAC), idempotency, retry logic with exponential backoff,
  payload validation, event routing, and dead letter queues.
